import React, { useState } from "react";

export const Accordion = ({ children }) => <div>{children}</div>;
export const AccordionItem = ({ value, children }) => <div>{children}</div>;
export const AccordionTrigger = ({ children }) => <button>{children}</button>;
export const AccordionContent = ({ children }) => <div>{children}</div>;
